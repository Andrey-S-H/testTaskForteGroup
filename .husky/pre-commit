#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running lint + pretier before commit..."
if ! npm run lint; then
  echo "❌ Commit blocked: fix lint errors first!"
  exit 1
fi

# echo "🧪 Running tests before commit..."
# if ! npm run test; then
#   echo "❌ Commit blocked: fix test errors first!"
#   exit 1
# fi

echo "✅ All checks passed, commit allowed."